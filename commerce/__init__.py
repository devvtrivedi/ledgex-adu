"""Intentionally empty. Scaffold only, for the same reason as core/__init__.py:
I15's "commerce must not import core.store" contract needs commerce/ to
be a real package to serve as a source_modules entry. No commerce
schema code (customer, disclosure, order, payment -- §13) belongs here
yet.
"""
